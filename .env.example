# Optional — only required to REFRESH club stats from the live API (Path 4 in README).
# Fresh clones can rebuild player awards without this key (uses committed player_club_2026.csv).
#
# API-Football (api-sports.io) — free tier: ~100 requests/day
# Get a key at https://www.api-football.com/
APIFOOTBALL_KEY=your_key_here
